info:
  name: fetch participants
  type: http
  seq: 3

http:
  method: GET
  url: "{{API_URL}}/chats/{{CHAT_NAME}}/participants"
  auth:
    type: bearer
    token: "{{A_JWT}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
